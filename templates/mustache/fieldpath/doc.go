/*
Package fieldpath implements a simple grammar for evaluating expressions,
using an intuitive dot-based syntax (foo.spec.bar).

It is more nil-tolerant than obvious alternatives.
*/

package fieldpath
